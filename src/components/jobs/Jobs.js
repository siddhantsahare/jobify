import React, { Fragment } from 'react';

import JobFilterByFullTime from './JobFilterByFullTime';

const Jobs = () => {
  return (
    <Fragment>
      <JobFilterByFullTime />
    </Fragment>
  );
};

export default Jobs;
